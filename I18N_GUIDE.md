# Инструкция по мультиязычности

## Что сделано

1. ✅ Добавлено поле `language` в модель `TelegramUser` (по умолчанию 'en')
2. ✅ Создана базовая структура переводов в `src/i18n/index.ts`
3. ✅ Созданы утилиты для работы с языком

## Что нужно сделать

1. Применить миграцию БД: `npm run db:push` (добавит поле language)
2. Обновить команды для использования переводов
3. Добавить команду `/language` для смены языка

## Применение миграции

```bash
npm run db:push
```

## Пример использования переводов в командах

```typescript
import { t } from '../i18n';
import { getUserLanguage } from '../utils/language';

// В команде:
const lang = await getUserLanguage(ctx.user.id);
const message = t(lang, 'stats.yourStats', { level: stats.level });
```

## Добавление новых переводов

1. Откройте `src/i18n/index.ts`
2. Добавьте ключи в объекты `en` и `ru`
3. Используйте `{параметр}` для подстановки значений

## Команда смены языка

Нужно добавить команду `/language` или `/lang` для смены языка пользователя.
