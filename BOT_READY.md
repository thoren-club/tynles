# Бот запущен! Что дальше?

## 1. Настройка автозапуска (важно!)

Чтобы бот запускался автоматически после перезагрузки сервера:

```bash
pm2 save
pm2 startup
```

PM2 покажет команду вида:
```bash
sudo env PATH=$PATH:/usr/bin pm2 startup systemd -u root --hp /root
```

**Скопируйте и выполните эту команду** (может отличаться в зависимости от вашего пользователя).

## 2. Проверка работы бота

### В Telegram:

1. Найдите вашего бота в Telegram (по имени, которое вы дали @BotFather)
2. Отправьте команду `/start`
3. Бот должен ответить приветственным сообщением

### На сервере:

```bash
# Просмотр логов в реальном времени
pm2 logs task-bot

# Статус процесса
pm2 status

# Последние логи
pm2 logs task-bot --lines 50
```

## 3. Основные команды PM2

```bash
pm2 status              # статус всех процессов
pm2 logs task-bot       # просмотр логов
pm2 restart task-bot    # перезапуск
pm2 stop task-bot       # остановка
pm2 delete task-bot     # удаление из списка
```

## 4. Основные команды бота

После успешного запуска попробуйте в Telegram:

- `/start` - приветствие
- `/space_create My Space` - создать пространство
- `/space_list` - список пространств
- `/task_add` - создать задачу (wizard)
- `/goal_add` - создать цель (wizard)
- `/me` - ваша статистика
- `/help` - список команд

Полный список команд в README.md

## 5. Если что-то не работает

### Бот не отвечает:

1. Проверьте логи: `pm2 logs task-bot`
2. Проверьте .env файл: `cat .env`
3. Проверьте что БД работает: `systemctl status postgresql`
4. Перезапустите: `pm2 restart task-bot`

### Ошибки в логах:

- Ошибки подключения к БД - проверьте DATABASE_URL в .env
- Ошибки BOT_TOKEN - проверьте токен
- Другие ошибки - посмотрите полные логи

## 6. Обновление бота в будущем

Когда нужно обновить код:

```bash
cd /root/tynles  # путь к проекту
git pull
npm install
npm run build
npm run db:push  # если были изменения в схеме
pm2 restart task-bot
```

## Поздравляем! 🎉

Бот готов к работе! Начните с создания пространства через `/space_create`.
