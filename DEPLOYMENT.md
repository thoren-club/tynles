# Инструкция по обновлению приложения на сервере

## Требования

- Node.js (версия 18 или выше)
- PostgreSQL (версия 14 или выше)
- npm или yarn
- Git
- PM2 (рекомендуется для production) или systemd

---

## Шаги обновления

### 1. Подготовка к обновлению

#### 1.1. Резервное копирование базы данных

```bash
# Создать резервную копию PostgreSQL
pg_dump -U <username> -d <database_name> > backup_$(date +%Y%m%d_%H%M%S).sql

# Или если используется docker:
docker exec <postgres_container> pg_dump -U <username> <database_name> > backup_$(date +%Y%m%d_%H%M%S).sql
```

#### 1.2. Остановка приложения

```bash
# Если используется PM2:
pm2 stop all

# Или если используется systemd:
sudo systemctl stop telegram-task-bot

# Или если запущено напрямую:
pkill -f "node dist/index.js"
```

---

### 2. Обновление кода

#### 2.1. Переход в директорию проекта

```bash
cd /path/to/your/project
```

#### 2.2. Получение последних изменений

```bash
# Если используется Git:
git fetch origin
git pull origin main

# Или если обновление через архив:
# Распакуйте новый архив в директорию проекта
```

#### 2.3. Проверка изменений

```bash
# Проверьте, что все файлы на месте
ls -la src/
ls -la web/src/
ls -la prisma/
```

---

### 3. Обновление зависимостей

#### 3.1. Backend зависимости

```bash
# Установка/обновление backend зависимостей
npm install

# Проверка обновлений
npm outdated
```

#### 3.2. Frontend зависимости

```bash
# Переход в директорию web
cd web

# Установка/обновление frontend зависимостей
npm install

# Возврат в корневую директорию
cd ..
```

---

### 4. Обновление базы данных

#### 4.1. Проверка миграций Prisma

```bash
# Проверить статус миграций
npx prisma migrate status

# Просмотр схемы
npx prisma format
```

#### 4.2. Применение миграций

**⚠️ ВНИМАНИЕ: Перед применением миграций убедитесь, что сделали резервную копию БД!**

```bash
# Генерация клиента Prisma
npx prisma generate

# Применение миграций (для разработки)
npx prisma migrate dev

# ИЛИ для production (без генерации SQL файлов):
npx prisma migrate deploy

# ИЛИ если нужно просто синхронизировать схему без миграций:
npx prisma db push
```

#### 4.3. Проверка подключения к БД

```bash
# Проверка подключения через Prisma Studio (опционально)
npx prisma studio
# Откройте в браузере http://localhost:5555

# Или проверьте через psql:
psql -U <username> -d <database_name> -c "\dt"
```

---

### 5. Сборка приложения

#### 5.1. Сборка Backend

```bash
# Компиляция TypeScript
npm run build

# Проверка, что dist/ создан
ls -la dist/
```

#### 5.2. Сборка Frontend

```bash
# Сборка React приложения
npm run build:frontend

# Или пошагово:
cd web
npm run build
cd ..

# Проверка, что web/dist/ создан
ls -la web/dist/
```

---

### 6. Настройка переменных окружения

#### 6.1. Проверка .env файла

```bash
# Убедитесь, что .env файл существует и содержит все необходимые переменные
cat .env
```

#### 6.2. Необходимые переменные окружения

```env
# Telegram Bot
BOT_TOKEN=your_bot_token_here

# Web App URL
WEB_APP_URL=https://your-domain.com

# Database
DATABASE_URL=postgresql://username:password@localhost:5432/database_name

# Telegram Secret Key (для проверки подписи)
TELEGRAM_SECRET_KEY=your_secret_key_here

# Опционально
BOT_ONLY=false  # true для запуска только бота (без веб-сервера)
NODE_ENV=production
PORT=3000  # Порт для веб-сервера
```

#### 6.3. Проверка переменных

```bash
# Проверка, что все переменные установлены
node -e "require('dotenv').config(); console.log(process.env.BOT_TOKEN ? 'BOT_TOKEN: OK' : 'BOT_TOKEN: MISSING');"
```

---

### 7. Запуск приложения

#### 7.1. Тестовый запуск (без PM2)

```bash
# Запуск в режиме разработки (для проверки)
npm run dev

# Или для production:
npm start
```

**Проверьте логи на наличие ошибок!**

#### 7.2. Запуск через PM2 (рекомендуется)

```bash
# Установка PM2 (если еще не установлен)
npm install -g pm2

# Запуск через PM2
pm2 start dist/index.js --name telegram-task-bot

# Или если есть ecosystem.config.js:
pm2 start ecosystem.config.js

# Сохранение конфигурации PM2
pm2 save

# Настройка автозапуска при перезагрузке системы
pm2 startup
# Выполните команду, которую PM2 выведет
```

#### 7.3. Запуск через systemd (альтернатива)

Создайте файл `/etc/systemd/system/telegram-task-bot.service`:

```ini
[Unit]
Description=Telegram Task Bot
After=network.target postgresql.service

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/your/project
Environment="NODE_ENV=production"
Environment="PATH=/usr/bin:/usr/local/bin"
ExecStart=/usr/bin/node dist/index.js
Restart=on-failure
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Затем:

```bash
# Перезагрузка systemd
sudo systemctl daemon-reload

# Запуск сервиса
sudo systemctl start telegram-task-bot

# Проверка статуса
sudo systemctl status telegram-task-bot

# Включение автозапуска
sudo systemctl enable telegram-task-bot
```

---

### 8. Проверка работы приложения

#### 8.1. Проверка Backend API

```bash
# Проверка здоровья API (если есть эндпоинт /health)
curl http://localhost:3000/api/health

# Или проверка через браузер:
# http://localhost:3000/api/stats/leaderboard (требует авторизации)
```

#### 8.2. Проверка Frontend

```bash
# Проверка, что статические файлы отдаются
curl http://localhost:3000/ | head -20

# Или откройте в браузере:
# http://localhost:3000
```

#### 8.3. Проверка Telegram Bot

```bash
# Проверка логов бота
pm2 logs telegram-task-bot

# Или если через systemd:
sudo journalctl -u telegram-task-bot -f

# Отправьте команду /start боту в Telegram
```

#### 8.4. Проверка WebApp

1. Откройте Telegram бота
2. Нажмите кнопку "🚀 Открыть приложение" в меню
3. Проверьте, что WebApp открывается и работает

---

### 9. Проверка Scheduled Tasks

#### 9.1. Проверка логов

```bash
# Просмотр логов PM2
pm2 logs telegram-task-bot --lines 100

# Или через systemd:
sudo journalctl -u telegram-task-bot -n 100
```

#### 9.2. Проверка cron задач

Приложение использует `setInterval` для:
- **Отправка напоминаний**: каждую минуту
- **Генерация историй**: каждый час (проверка начала недели)
- **Обработка просроченных задач**: каждый час (в начале часа)

Убедитесь, что в логах нет ошибок от этих задач.

---

### 10. Настройка Nginx (если используется)

Если вы используете Nginx как reverse proxy:

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

После изменения конфигурации:

```bash
# Проверка конфигурации
sudo nginx -t

# Перезагрузка Nginx
sudo systemctl reload nginx
```

---

### 11. Мониторинг и обслуживание

#### 11.1. Мониторинг PM2

```bash
# Статус процессов
pm2 status

# Мониторинг в реальном времени
pm2 monit

# Просмотр информации о процессе
pm2 describe telegram-task-bot
```

#### 11.2. Мониторинг логов

```bash
# Просмотр последних 100 строк логов
pm2 logs telegram-task-bot --lines 100

# Следить за логами в реальном времени
pm2 logs telegram-task-bot --raw
```

#### 11.3. Мониторинг базы данных

```bash
# Проверка размера БД
psql -U <username> -d <database_name> -c "SELECT pg_size_pretty(pg_database_size('<database_name>'));"

# Проверка активных подключений
psql -U <username> -d <database_name> -c "SELECT count(*) FROM pg_stat_activity;"
```

---

### 12. Откат изменений (если что-то пошло не так)

#### 12.1. Быстрый откат через Git

```bash
# Возврат к предыдущей версии
git log --oneline -10  # Найти commit hash предыдущей версии
git checkout <previous_commit_hash>

# Пересборка и перезапуск
npm run build
npm run build:frontend
pm2 restart telegram-task-bot
```

#### 12.2. Откат базы данных

```bash
# Восстановление из резервной копии
psql -U <username> -d <database_name> < backup_YYYYMMDD_HHMMSS.sql

# Или через docker:
docker exec -i <postgres_container> psql -U <username> <database_name> < backup_YYYYMMDD_HHMMSS.sql
```

---

### 13. Проверочный чеклист после обновления

- [ ] Резервная копия БД создана
- [ ] Код обновлен
- [ ] Зависимости установлены
- [ ] База данных обновлена (миграции применены)
- [ ] Backend собран (`dist/` существует)
- [ ] Frontend собран (`web/dist/` существует)
- [ ] Переменные окружения проверены
- [ ] Приложение запущено без ошибок
- [ ] API отвечает
- [ ] Frontend открывается
- [ ] Telegram Bot отвечает на команды
- [ ] WebApp открывается в Telegram
- [ ] Scheduled tasks работают (проверка логов)
- [ ] Мониторинг настроен

---

## Частые проблемы и решения

### Проблема: "Cannot find module 'prisma'"

**Решение:**
```bash
npm install
npx prisma generate
```

### Проблема: "Database connection error"

**Решение:**
1. Проверьте `DATABASE_URL` в `.env`
2. Проверьте, что PostgreSQL запущен: `sudo systemctl status postgresql`
3. Проверьте доступность БД: `psql -U <username> -d <database_name>`

### Проблема: "Port 3000 is already in use"

**Решение:**
```bash
# Найти процесс на порту 3000
lsof -i :3000

# Остановить процесс
kill -9 <PID>

# Или использовать другой порт через .env:
PORT=3001
```

### Проблема: "Frontend не открывается"

**Решение:**
1. Проверьте, что `web/dist/` существует и содержит файлы
2. Проверьте, что Express отдает статические файлы из `web/dist/`
3. Проверьте логи веб-сервера на ошибки

### Проблема: "Bot не отвечает"

**Решение:**
1. Проверьте `BOT_TOKEN` в `.env`
2. Проверьте логи бота: `pm2 logs telegram-task-bot`
3. Проверьте, что бот запущен: `pm2 status`

---

## Контакты и поддержка

При возникновении проблем:
1. Проверьте логи: `pm2 logs telegram-task-bot`
2. Проверьте статус: `pm2 status`
3. Проверьте базу данных: подключение и миграции
4. Проверьте переменные окружения: `.env` файл

---

**Дата создания инструкции**: $(date)
**Версия приложения**: 1.0.0
