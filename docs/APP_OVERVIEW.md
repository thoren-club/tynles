# Обзор приложения - Полная документация

## Архитектура приложения

### Технологический стек
- **Backend**: Node.js + Express + TypeScript + Prisma ORM + PostgreSQL
- **Frontend**: React + TypeScript + Vite + React Router DOM
- **Bot**: Grammy (Telegram Bot Framework)
- **База данных**: PostgreSQL с Prisma ORM

---

## Структура проекта

```
/
├── src/                    # Backend код
│   ├── commands/          # Telegram bot команды
│   ├── web/               # Web API (Express)
│   │   └── api/           # API роуты
│   ├── utils/             # Утилиты
│   ├── index.ts           # Основной файл (бот + веб-сервер)
│   └── db.ts              # Prisma клиент
├── web/                   # Frontend код
│   ├── src/
│   │   ├── pages/         # React страницы
│   │   ├── components/    # React компоненты
│   │   └── api/           # API клиент
│   └── dist/              # Собранный фронтенд
└── prisma/                # Prisma schema
```

---

## Фронтенд (React WebApp)

### Страницы и маршруты

#### 1. **Dashboard** (`/`)
- **Функционал**:
  - Верхняя панель: уровень с прогресс-баром, аватарка, настройки, уведомления
  - Карусель историй (Stories) с индикаторами просмотра
  - Статистика задач на сегодня (только ежедневные повторяющиеся задачи)
  - Список актуальных задач с детальной информацией
  - Задачи с важностью (цветовая индикация), дедлайнами, XP
  - Undo функционал с таймером (5 секунд) после выполнения задачи

#### 2. **Deals** (`/deals`) - "Дела"
- **Функционал**:
  - Секция целей (макс. 6 на экране, кнопка "Все цели" если больше)
  - Секция задач с типами: одноразовые, ежедневные, еженедельные
  - Кнопка "Создать" с dropdown (Цель/Задача)
  - Модальная форма создания с свайпом вниз для закрытия
  - Поля формы:
    - **Для цели**: название, описание, дедлайн, важность, тип (год/месяц/бессрочная)
    - **Для задачи**: название, описание, дедлайн, важность, повторяющаяся (чекбокс), дни недели

#### 3. **Leaderboard** (`/leaderboard`)
- **Функционал**:
  - Табы: "Глобальный" и "Название пространства"
  - Глобальный лидерборд: показывает 12 лиг, позиции, уровень, XP
  - Лидерборд пространства: показывает количество выполненных задач за 30 дней
  - Система лиг (12 уровней): Бронзовая → ... → Легендарная

#### 4. **Spaces** (`/spaces`)
- **Функционал**:
  - Список всех пространств пользователя
  - Активное пространство выделено
  - Кнопка "Создать пространство" (макс. 3 пространства)
  - Настройки пространства (иконка шестерёнки):
    - Участники пространства (роли: Viewer, Editor, Admin)
    - Изменение ролей участников (только для Admin)
    - Код приглашения
    - Награды уровней (редактирование для Admin)

#### 5. **Profile** (`/profile`)
- **Функционал**:
  - Аватарка пользователя
  - Редактирование имени (с кнопками Сохранить/Отмена)
  - ID Telegram (только чтение)
  - Роль пользователя (Разработчик/Премиум/Обычный)

#### 6. **Settings** (`/settings`)
- **Функционал**:
  - Страница настроек с заголовками и подзаголовками (заглушка)

#### 7. **Level Progression** (`/level-progression`)
- **Функционал**:
  - Отображение всех 80 уровней
  - Награды для каждого уровня (загружаются с сервера)
  - Возврат на главную страницу

#### 8. **Goal Detail** (`/goal/:id`)
- **Функционал**:
  - Детальная информация о цели (название, описание, дедлайн, важность, тип)
  - Кнопки: выполнить/отменить, удалить
  - Открывается как шторка снизу вверх

#### 9. **All Goals** (`/all-goals`)
- **Функционал**:
  - Все цели, разделенные на текущие и выполненные
  - Фильтры: все, год, месяц, бессрочные

### Компоненты

#### **Layout**
- Нижняя навигация (Navbar):
  - Dashboard (📊)
  - Deals (📋)
  - Leaderboard (🏆)
  - Spaces (📁)
- Скрывается на страницах: `/profile`, `/settings`, `/level-progression`, `/goal`, `/all-goals`, `/space-settings`

---

## Backend (Express API + Telegram Bot)

### API Роуты

#### **Auth** (`/api/auth`)
- `GET /me` - Получить информацию о текущем пользователе (включая роль)
- `PUT /me` - Обновить имя пользователя
- `GET /spaces` - Получить список пространств пользователя
- `POST /invites/use` - Использовать код приглашения
- `POST /spaces/:spaceId/switch` - Переключить активное пространство

#### **Spaces** (`/api/spaces`)
- `GET /current` - Получить текущее пространство
- `POST /create` - Создать новое пространство
- `POST /:spaceId/switch` - Переключить пространство
- `GET /current/leaderboard` - Лидерборд пространства (30 дней)
- `GET /current/rewards` - Получить награды уровней для пространства
- `PUT /current/rewards/:level` - Обновить награду уровня (только Admin)

#### **Tasks** (`/api/tasks`)
- `GET /` - Получить все задачи текущего пространства
- `POST /` - Создать задачу (с автоматическим расчетом XP)
- `POST /:taskId/complete` - Выполнить задачу (начисление XP, пересчет уровня)
- `DELETE /:taskId` - Удалить задачу

#### **Goals** (`/api/goals`)
- `GET /` - Получить все цели текущего пространства
- `POST /` - Создать цель
- `POST /:goalId/toggle` - Переключить выполнение цели (начисление XP при выполнении)
- `DELETE /:goalId` - Удалить цель

#### **Members** (`/api/members`)
- `GET /` - Получить участников пространства
- `POST /invites` - Создать код приглашения
- `PUT /:userId/role` - Изменить роль участника (только Admin)

#### **Stats** (`/api/stats`)
- `GET /me` - Получить статистику пользователя
- `GET /leaderboard` - Получить глобальный лидерборд с информацией о лигах

#### **Stories** (`/api/stories`)
- `GET /` - Получить истории для пользователя (автоматически генерируемые каждую неделю)

---

## Утилиты (Backend)

### 1. **XP и уровни** (`src/utils/xp.ts`)
- `addXp()` - Добавить XP пользователю с пересчетом уровня
- `getXpProgress()` - Получить прогресс до следующего уровня
- `getProgressBar()` - Визуализация прогресс-бара

### 2. **Расчет XP** (`src/types.ts`)
- `calculateTaskXp(difficulty, recurrenceType)` - Расчет XP для задачи:
  - **Важность (difficulty)**:
    - 1 = 10 XP (Не обязательно)
    - 2 = 25 XP (Можно не торопиться)
    - 3 = 50 XP (Нужно торопиться)
    - 4 = 75 XP (Подпекает)
  - **Модификаторы повторения**:
    - `daily` (ежедневные): +10% опыта
    - `weekly` (еженедельные): +5% опыта
    - `none` (одноразовые): без модификатора
- `calculateLevel(totalXp)` - Расчет уровня на основе XP:
  - Прогрессия: каждый уровень требует на 2% больше XP
  - Базовый XP за уровень: 100 XP
  - Максимальный уровень: 80

### 3. **Система лиг** (`src/utils/leagues.ts`)
- 12 лиг: Бронзовая → Серебряная → Золотая → ... → Легендарная
- Период лиги: 30 дней
- Распределение:
  - Первые 10 мест → переход вперёд
  - Следующие 30 мест → остаются
  - Остальные 10 мест → переход назад
- Функции:
  - `getUserCurrentLeague()` - Получить текущую лигу пользователя
  - `getUserLeaguePosition()` - Получить позицию в лиге
  - `updateLeaguesForSpace()` - Обновить лиги для пространства

### 4. **Роли пользователей** (`src/utils/user-role.ts`)
- Типы ролей:
  - `разработчик` (Developer)
  - `премиум` (Premium)
  - `обычный` (Regular)
- `getUserRole(tgId)` - Определение роли (пока на основе hardcoded списка разработчиков)

### 5. **Сгорание задач** (`src/utils/task-expiration.ts`)
- `processExpiredRecurringTasks()` - Обработка просроченных повторяющихся задач:
  - Штраф: -50% XP от назначенной задачи
  - Автоматическое переназначение на следующий период
  - Удаление, если нет следующего периода

### 6. **Генерация историй** (`src/utils/story-generator.ts`)
- `generateWeeklyStories()` - Автоматическая генерация историй каждую неделю:
  - Статистика: выполненные задачи, полученные уровни, изменение позиции в лидерборде
  - Хранение базовых данных (baseLevel, baseXp, leaderboardPosition) для сравнения

### 7. **Повторяющиеся задачи** (`src/utils/recurrence.ts`)
- `calculateNextDueDate()` - Расчет следующей даты для повторяющейся задачи
- Поддержка типов: `daily`, `weekly`, `monthly`

---

## Scheduled Tasks (Cron Jobs)

### В `src/index.ts`:

1. **Отправка напоминаний о задачах**
   - Запуск: каждую минуту
   - Функция: `sendReminders()`

2. **Генерация еженедельных историй**
   - Запуск: каждый час (проверка начала недели - воскресенье 23:00 или понедельник 01:00)
   - Функция: `generateWeeklyStories()`
   - Защита от повторного запуска в один день

3. **Обработка просроченных повторяющихся задач**
   - Запуск: каждый час (в начале часа, минута = 0)
   - Функция: `processExpiredRecurringTasks()`

---

## База данных (Prisma Schema)

### Основные модели:

- **TelegramUser** - Пользователи Telegram
- **Space** - Пространства
- **SpaceMember** - Участники пространств (роли: Viewer, Editor, Admin)
- **Task** - Задачи (с поддержкой повторяющихся)
- **Goal** - Цели
- **UserSpaceStats** - Статистика пользователя в пространстве (XP, уровень, лига)
- **Story** - Истории (еженедельные, админские)
- **Reward** - Награды уровней для пространства
- **Invite** - Коды приглашений

---

## Логика работы приложения

### 1. **Аутентификация**
- Использует Telegram WebApp `initData`
- Проверка подписи на backend
- Middleware `authMiddleware` для всех защищенных роутов

### 2. **Процесс создания задачи**
1. Пользователь заполняет форму в Deals
2. Frontend отправляет данные в `POST /api/tasks`
3. Backend автоматически рассчитывает XP на основе важности и типа повторения
4. Создается задача в БД
5. Frontend обновляет список задач

### 3. **Процесс выполнения задачи**
1. Пользователь нажимает чекбокс на Dashboard/Deals
2. Frontend отправляет `POST /api/tasks/:taskId/complete`
3. Backend:
   - Начисляет XP через `addXp()`
   - Пересчитывает уровень через `calculateLevel()`
   - Удаляет задачу (или помечает как выполненную)
4. Frontend показывает временный undo (5 секунд)
5. После истечения таймера изменения фиксируются

### 4. **Система лиг**
- Каждые 30 дней пересчитываются лиги
- Позиция определяется на основе XP, заработанного за 30 дней
- Обновление через `updateLeaguesForSpace()`

### 5. **Истории**
- Автоматическая генерация каждую неделю (воскресенье/понедельник)
- Хранят статистику: выполненные задачи, полученные уровни, изменение в лидерборде
- Отображаются в карусели на Dashboard

---

## Особенности реализации

### XP и уровни
- **Базовый расчет**: важность определяет базовый XP
- **Модификаторы**: повторяющиеся задачи дают больше XP
- **Прогрессия**: каждый уровень требует на 2% больше XP

### Повторяющиеся задачи
- Ежедневные: `recurrenceType = 'daily'`, `daysOfWeek = [0,1,2,3,4,5,6]`
- Еженедельные: `recurrenceType = 'weekly'`, `daysOfWeek = [выбранные дни]`
- Одноразовые: `recurrenceType = null`

### Сгорание задач
- Если повторяющаяся задача не выполнена до дедлайна:
  - Штраф: -50% XP
  - Автоматическое переназначение на следующий период
  - Проверка каждый час

### Роли в пространствах
- **Admin**: может изменять роли участников, редактировать награды уровней
- **Editor**: может редактировать задачи/цели
- **Viewer**: только просмотр

---

## API Клиент (Frontend)

### Основные методы:
- `setAuthHeader(initData)` - Установка заголовка аутентификации
- `getUser()` - Получить пользователя
- `updateUserName(name)` - Обновить имя
- `getSpaces()` - Получить пространства
- `getTasks()` - Получить задачи
- `createTask(data)` - Создать задачу
- `completeTask(taskId)` - Выполнить задачу
- `getGoals()` - Получить цели
- `createGoal(data)` - Создать цель
- `getLeaderboard()` - Глобальный лидерборд
- `getSpaceLeaderboard()` - Лидерборд пространства
- `getStories()` - Получить истории
- `getLevelRewards()` - Получить награды уровней
- `updateLevelReward(level, text)` - Обновить награду (Admin)

---

## Файлы конфигурации

- `src/config.ts` - Конфигурация (токен бота, URL WebApp, БД)
- `prisma/schema.prisma` - Схема базы данных
- `web/vite.config.ts` - Конфигурация Vite
- `tsconfig.json` - Конфигурация TypeScript

---

## Дополнительные детали

### Telegram Bot
- Команды: `/start`, `/help`, и другие команды для управления задачами/целями
- Кнопка Mini App в меню для открытия WebApp

### Обработка ошибок
- Все API роуты имеют try/catch
- Frontend показывает alert при ошибках
- Логирование через `logger` (pino)

### Безопасность
- Проверка прав доступа для Admin действий
- Валидация входных данных
- Проверка принадлежности к пространству

---

## Известные ограничения / TODO

1. **База данных**:
   - Некоторые поля в формах (description, deadline для Goal) пока не сохраняются в БД (требуется миграция)
   - Роли пользователей определяются через hardcoded список (требуется миграция для хранения в БД)

2. **Функционал**:
   - Уведомления пока заглушка
   - Полный функционал просмотра историй (пролистывание) не реализован
   - Система лиг работает, но требует миграции БД для полного функционала

3. **UI/UX**:
   - Анимации свайпа реализованы только для создания задачи/цели
   - Можно улучшить индикаторы загрузки

---

## Технические детали

### Переменные окружения
- `BOT_TOKEN` - Токен Telegram бота
- `WEB_APP_URL` - URL WebApp
- `DATABASE_URL` - URL подключения к PostgreSQL
- `TELEGRAM_SECRET_KEY` - Секретный ключ для проверки подписи Telegram

### Порты
- Backend API: `3000` (по умолчанию)
- Frontend Dev Server: `5173` (Vite)
- Frontend Production: собирается в `web/dist/`, обслуживается через Express static

---

**Дата создания документа**: $(date)
**Версия приложения**: 1.0.0
