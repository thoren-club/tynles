# Design Guidelines — Gamified UI (Duolingo-style)

> **Важно**: Этот документ определяет визуальный стиль и поведение всех UI компонентов. Все новые компоненты и изменения должны соответствовать этому гайдлайну. При разработке всегда обращайтесь к этому файлу для проверки соответствия дизайну.

## 🎨 Философия дизайна

Приложение должно выглядеть **игровым**, **энергичным** и **дружелюбным** — в стиле Duolingo:
- **Яркие, контрастные цвета** для акцентов
- **Мягкие, закругленные формы** (крупные радиусы)
- **Плавные анимации**
- **Тактильная обратная связь** на каждое взаимодействие
- **Минимализм с акцентами**: тёмный фон, яркие кнопки и карточки

---

## 🎨 Цветовая система

### Основные цвета

#### Primary (Оранжевый)
- **Base**: `#FF8D00` (`--color-primary-500`)
- **Использование**: Основные кнопки, активные элементы, прогресс-бары, акценты
- **Градации**: От `--color-primary-50` (самый светлый) до `--color-primary-900` (самый тёмный)
- **Текст на primary**: `--color-neutral-950` (тёмный, #111019) для контраста

#### Background (Тёмный)
- **Base**: `#111019` (`--color-bg`, `--color-neutral-950`)
- **Surface**: `--color-neutral-900` (`#1a1826`) — карточки, модалки
- **Surface-2**: `--color-neutral-800` (`#2e2a44`) — вложенные элементы

#### Neutral (От белого к тёмному)
- **0-950**: Градация от `#ffffff` до `#111019`
- **Текст**: `--color-text` (`#f4f2ff`) — основной текст
- **Текст muted**: `--color-text-muted` (`#a09abf`) — вторичный текст, подписи

#### Semantic
- **Success**: `--color-success-500` (`#22c55e`) — успешные действия, выполненные задачи
- **Danger**: `--color-danger-500` (`#ff4d4f`) — удаление, ошибки, просроченные задачи

### Правила использования цветов

1. **Primary используется только для**:
   - Кнопок действий (`btn-primary`)
   - Активных табов, выбранных опций
   - Прогресс-баров и индикаторов прогресса
   - Аватаров по умолчанию
   - Иконок навигации (активное состояние)

2. **Neutral используется для**:
   - Фона карточек (`surface`, `surface-2`)
   - Границ (`border`, `border-strong`)
   - Неактивных элементов
   - Плейсхолдеров

3. **Success/Danger только для**:
   - Success: выполненные задачи, позитивная статистика
   - Danger: удаление, ошибки, предупреждения, просроченные задачи

---

## 🔲 Скругления (Border Radius)

### Принцип
Все элементы должны иметь **мягкие, закругленные углы**. Исключение: только системные элементы (например, иконки без фона).

### Токены

- `--radius-xs`: **8px** — маленькие бейджи, индикаторы, небольшие кнопки (например, день недели)
- `--radius-sm`: **10px** — кнопки, поля ввода (input), небольшие карточки
- `--radius-md`: **12px** — стандартные карточки (task-card, goal-card), модальные окна
- `--radius-lg`: **16px** — крупные карточки, секции
- `--radius-xl`: **20px** — модальные окна, большие блоки контента
- `--radius-pill`: **999px** — полностью круглые элементы (аватары, переключатели, табы)

### Примеры применения

- **Кнопки**: `--radius-sm` (10px) или `--radius-md` (12px) для крупных
- **Карточки задач/целей**: `--radius-md` (12px)
- **Поля ввода**: `--radius-sm` (10px)
- **Бейджи важности**: `--radius-xs` (8px)
- **Аватары**: `--radius-pill` (999px)
- **Табы/переключатели**: `--radius-pill` (999px) или `--radius-sm` (10px)

---

## 📐 Границы (Borders)

### Токены

- `--border-1`: `1px solid var(--color-border)` — стандартная граница (прозрачная с цветом)
- `--border-1-strong`: `1px solid var(--color-border-strong)` — более заметная граница

### Использование

- **Обёртки и карточки**: **без границ**. Отделение от фона делаем за счёт `background` и мягких `box-shadow`
- **Формы и интерактив**: `border: var(--border-1)` — только для input/select/textarea и мелких контролов
- **Активные элементы**: граница может менять цвет на `--color-primary-500` при hover/focus (только для контролов)

---

## ✨ Анимации и транзишены

### Принцип
Все анимации должны быть **плавными, упругими** и создавать ощущение **тактильности**. Время анимаций: **200-300ms** для стандартных переходов, **100-150ms** для микровзаимодействий.

### Длительности

- **Быстрые микровзаимодействия**: `100ms`, `150ms` (hover эффекты, клики)
- **Стандартные переходы**: `200ms`, `250ms` (изменение цвета, transform)
- **Медленные/важные**: `300ms` (появление модалок, крупные изменения)

### Timing functions

```css
/* Стандартный плавный переход */
transition: all 0.2s ease-out;

/* Упругий эффект (для карточек, кнопок при клике) */
transition: all 0.25s cubic-bezier(0.34, 1.56, 0.64, 1);

/* Мгновенный для hover (быстрая обратная связь) */
transition: opacity 0.15s ease-in-out;
```

### Эффекты

**ВАЖНО**: Все hover эффекты должны быть обернуты в `@media (hover: hover) and (pointer: fine)` для исключения на мобильных устройствах!

1. **Hover на кнопках**:
   ```css
   button {
     transition: all 0.2s ease-out;
   }
   
   @media (hover: hover) and (pointer: fine) {
     button:hover {
       opacity: 0.9;
       transform: translateY(-1px);
     }
   }
   ```

2. **Active (клик)**:
   ```css
   button:active {
     transform: scale(0.98); /* небольшое сжатие */
   }
   ```

3. **Карточки при hover**:
   ```css
   .card {
     transition: all 0.2s ease-out;
   }
   
   @media (hover: hover) and (pointer: fine) {
     .card:hover {
       transform: translateY(-2px);
       box-shadow: 0 10px 24px rgba(0, 0, 0, 0.22);
     }
   }
   ```

4. **Появление элементов**:
   ```css
   @keyframes fadeIn {
     from { opacity: 0; transform: translateY(8px); }
     to { opacity: 1; transform: translateY(0); }
   }
   ```

5. **Прогресс-бары**:
   ```css
   .progress-fill {
     transition: width 0.3s ease-out; /* плавное заполнение */
   }
   ```

### Accessibility

Всегда учитывайте `prefers-reduced-motion`:
```css
@media (prefers-reduced-motion: reduce) {
  * {
    animation-duration: 0.01ms !important;
    transition-duration: 0.01ms !important;
  }
}
```

---

## 📱 Типографика

### Шрифт
**Ubuntu** — единый шрифт для всего приложения. Загружается локально из `@fontsource/ubuntu`.

### Размеры

- `--text-xs`: **11px** — мелкие подписи, метаданные
- `--text-sm`: **13px** — вторичный текст, описания
- `--text-md`: **15px** — основной текст, кнопки
- `--text-lg`: **18px** — заголовки секций
- `--text-xl`: **22px** — крупные заголовки

### Веса

- `--fw-regular`: **400** — основной текст
- `--fw-medium`: **500** — подзаголовки, акцентный текст
- `--fw-semibold`: **600** — заголовки секций
- `--fw-bold`: **700** — крупные заголовки, акценты

### Line Height

- `--lh-tight`: **1.15** — заголовки
- `--lh-normal`: **1.35** — основной текст
- `--lh-relaxed`: **1.5** — длинный текст, описания

---

## 🎯 Компоненты

### Кнопки

#### Primary Button
- **Цвет фона**: `--color-primary-500`
- **Цвет текста**: `--tg-theme-button-text-color` (тёмный)
- **Радиус**: `--radius-sm` (10px)
- **Padding**: `8px 16px` (маленькие), `12px 20px` (средние), `14px 24px` (большие)
- **Hover**: `opacity: 0.9`, `transform: translateY(-1px)`
- **Active**: `transform: scale(0.98)`

#### Secondary Button
- **Цвет фона**: `--color-surface-2`
- **Цвет текста**: `--color-text`
- **Граница**: `var(--border-1)`
- **Радиус**: `--radius-sm` (10px)
- **Hover**: `opacity: 0.8`

#### Danger Button
- **Цвет фона**: `--color-danger-500`
- **Цвет текста**: `--color-neutral-0`
- **Радиус**: `--radius-sm` (10px)

### Поля ввода (Input)

- **Фон**: `--color-surface-2`
- **Цвет текста**: `--color-text`
- **Граница**: `var(--border-1)`
- **Радиус**: `--radius-sm` (10px)
- **Padding**: `12px 16px`
- **Focus**: граница меняется на `--color-primary-500`, `box-shadow: 0 0 0 3px color-mix(in srgb, var(--color-primary-500) 18%, transparent)`
- **Placeholder**: `--color-text-muted`

### DateTimePicker с пресетами

- **Пресет-кнопки**: 3 кнопки "Сегодня", "Завтра", "Через неделю"
- **Фон кнопок**: `--color-surface-2`, при hover — `--color-surface`
- **Border при hover**: `--color-primary-500`
- **Автозаполнение**: устанавливает дату + время 23:59
- **Input**: поддерживает `datetime-local` для выбора даты и времени

### Селектор важности (ImportanceSelector)

- **Формат**: 4 кнопки в виде сетки 2×2
- **Иконки**: 🔵 Низкая | 🟢 Средняя | 🟠 Высокая | 🔴 Критическая
- **Активное состояние**: цветной фон и утолщенная граница (2px)
- **Анимация**: bounce эффект при hover (`cubic-bezier(0.34, 1.56, 0.64, 1)`)
- **Цвета активных состояний**:
  - Низкая: серый (`--color-text-muted`)
  - Средняя: зеленый (`--color-success-500`)
  - Высокая: оранжевый (`--color-primary-500`)
  - Критическая: красный (`--color-danger-500`)

### Пресеты повторяющихся задач (RecurringPresets)

- **Быстрые пресеты**: 3 кнопки в ряд
  - 🔄 Каждый день (7 дней)
  - 💼 Будни (ПН-ПТ)
  - 🏖️ Выходные (СБ-ВС)
- **Ручной выбор**: 7 кнопок для дней недели (ПН-ВС)
- **Активные пресеты**: `--color-primary-500` фон
- **Min-height пресетов**: 68px для удобства нажатия

### Карточки (Cards)

- **Фон**: `--color-surface`
- **Граница**: `var(--border-1)`
- **Радиус**: `--radius-md` (12px)
- **Padding**: `16px`
- **Hover**: `transform: translateY(-2px)`, тень увеличивается

### Модальные окна / Шторки

- **Фон overlay**: `rgba(0, 0, 0, 0.5)`
- **Фон контента**: `--color-surface`
- **Радиус верхних углов**: `20px 20px 0 0` (для bottom sheet)
- **Анимация**: `slideUp` с `ease-out`

### Прогресс-бары

- **Фон**: `--color-surface-2`
- **Fill**: `--color-primary-500`
- **Радиус**: `4px` или `999px` (pill)
- **Высота**: `8px` (маленький), `12px` (средний)
- **Анимация fill**: `transition: width 0.3s ease-out`

### Бейджи / Индикаторы

- **Радиус**: `--radius-xs` (8px) или `--radius-pill` (999px)
- **Padding**: `4px 8px` (маленькие), `6px 12px` (средние)
- **Важность**:
  - Low: `--color-text-muted`, фон через `color-mix`
  - Medium: `--color-success-500`
  - High: `--color-primary-500`
  - Urgent: `--color-danger-500`

---

## 🎮 Игровые элементы

### Аватары

- **Размеры**: 40px, 48px, 64px, 100px
- **Радиус**: `--radius-pill` (999px) — всегда круглые
- **Фон по умолчанию**: `--color-primary-500`
- **Цвет текста**: `--tg-theme-button-text-color`

### Уровни / Прогресс

- **Иконка уровня**: круг с числом, `--color-primary-500`, крупный шрифт (20px+)
- **Прогресс-бар**: всегда с `--color-primary-500` fill
- **XP текст**: `--color-primary-500` для акцентов

### Графики / Чарты

- **Цвет линий/столбцов**: `--color-primary-500`
- **Фон**: `--color-surface` или `--color-surface-2`
- **Радиус элементов графика**: `--radius-sm` (10px)
- **Анимация появления**: плавная, `0.3s ease-out`

---

## 📐 Spacing (отступы)

### Токены (через CSS переменные)

- `--spacing-xs`: **3px** — минимальные отступы
- `--spacing-sm`: **6px** — маленькие отступы
- `--spacing-md`: **10px** — стандартные отступы
- `--spacing-lg`: **14px** — крупные отступы
- `--spacing-xl`: **18px** — между секциями
- `--spacing-2xl`: **22px** — между секциями
- `--spacing-3xl`: **28px** — между крупными блоками
- `--spacing-4xl`: **36px** — максимальные отступы

### Правила

- **Внутри карточки**: `var(--spacing-lg)` (14px) padding
- **Между карточками**: `var(--spacing-md)` (10px) gap
- **Между секциями**: `var(--spacing-2xl)` (22px) или `var(--spacing-3xl)` (28px)
- **Внутри форм**: `var(--spacing-sm)` до `var(--spacing-md)` (6-10px) gap между полями
- **Ширина контента**: максимум `var(--content-max-width)` (680px)

---

## 🎨 Тени (Shadows)

### Box Shadow

```css
/* Стандартная карточка */
box-shadow: 0 2px 8px rgba(0, 0, 0, 0.12);

/* Hover карточки */
box-shadow: 0 10px 24px rgba(0, 0, 0, 0.22);

/* Модальные окна */
box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
```

---

## 🧱 Wrapper tiers (Z‑позиции)

Для вложенных оберток используем фиксированные уровни, каждый следующий — светлее:

- `--color-wrapper-1`: базовый фон обёртки
- `--color-wrapper-2`: слой выше
- `--color-wrapper-3`: ещё выше
- `--shadow-wrapper`: базовая «ступенька» для обёрток (`0 4px 0 #171620`)

Правило: чем выше «z‑уровень» обёртки, тем светлее фон.

---

## ✅ Чеклист при разработке компонента

- [ ] Используются токены цветов (`--color-*`) вместо хардкода
- [ ] Используются токены радиусов (`--radius-*`)
- [ ] Используются токены границ (`--border-*`)
- [ ] Добавлены плавные транзишены (200-300ms)
- [ ] Добавлены hover/active эффекты с transform
- [ ] Hover эффекты обернуты в `@media (hover: hover) and (pointer: fine)`
- [ ] Учтён `prefers-reduced-motion`
- [ ] Шрифт Ubuntu используется везде
- [ ] Тексты используют i18n (`tr()` или `t()`)
- [ ] Карточки имеют границу и тень
- [ ] Кнопки имеют hover/active состояния

## 🎯 Переиспользуемые UI компоненты

Для обеспечения единообразия всегда используйте готовые компоненты из `web/src/components/ui`:

- **Button** — универсальные кнопки с вариантами `primary`, `secondary`, `danger`, `success`
- **Input** — текстовые поля ввода
- **DateTimePickerWithPresets** — выбор даты/времени с быстрыми пресетами
- **ImportanceSelector** — визуальный выбор важности (4 кнопки с иконками)
- **RecurringPresets** — пресеты для повторяющихся задач + ручной выбор дней
- **Dropdown** — выпадающий список
- **Skeleton** — скелетоны для загрузки
- **BottomSheet** — шторка снизу для модальных окон

---

## 📚 Ссылки на токены

Все токены определены в `web/src/styles/tokens.css`. Используйте их вместо хардкода:

- Цвета: `--color-*`
- Радиусы: `--radius-*`
- Границы: `--border-*`
- Типографика: `--text-*`, `--font-*`, `--fw-*`, `--lh-*`

---

**Последнее обновление**: 2026-01-18
**Версия**: 1.0.0
